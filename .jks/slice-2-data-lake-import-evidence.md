# Slice 2 Evidence: Data Lake Import

Timestamp: 2026-05-18T22:30:37-04:00

## Scope

Imported the completed V1 Gold Coast data-lake artifacts into the monorepo boundaries:

- App code and tests: `apps/data-lake/`
- Acceptance SQL: `sql/data-lake/acceptance/`
- Data-lake docs: `docs/ops/data-lake/`

This slice stayed import-only. It did not create or modify AWS resources, run live GHL extraction, run GHL write paths, deploy infrastructure, or push to GitHub.

## Imported Artifacts

- `apps/data-lake/pyproject.toml`
- `apps/data-lake/scripts/build_curated_tables.py`
- `apps/data-lake/scripts/ghl_extract_raw.py`
- `apps/data-lake/src/gold_coast_data_lake/`
- `apps/data-lake/tests/`
- `sql/data-lake/acceptance/*.sql`
- `docs/ops/data-lake/*.md`
- `docs/ops/data-lake/schema.yml`

Generated raw extracts, curated Parquet output, checkpoint files, manifests, recordings, and bytecode caches were not imported.

## Intentional Import Adjustments

- Updated docs to reference the new monorepo paths under `apps/data-lake/`, `sql/data-lake/`, and `docs/ops/data-lake/`.
- Updated `apps/data-lake/scripts/ghl_extract_raw.py` so local extraction requires `--env-file` or `GHL_ENV_FILE` instead of baking in the old workspace credential path.
- Added gitignore coverage for local data-lake runtime outputs and Python cache/venv artifacts.

## Verification

- Source reconciliation:
  - Compared V1 `scripts/`, `src/`, `tests/`, `sql/acceptance/`, and `docs/` against imported targets.
  - Differences were limited to generated bytecode caches before cleanup, new monorepo-relative paths in docs, and the credential-path safety adjustment in `ghl_extract_raw.py`.
- Read-only GHL guard:
  - `rg` scan for `.post(`, `.put(`, `.patch(`, `.delete(`, and direct non-GET helper calls under `apps/data-lake/` returned no matches.
  - Existing test `test_client_refuses_non_get_methods` passed.
- Secret hygiene:
  - Bounded credential/secret keyword scan found env var names, placeholder credential paths, and secret-exclusion comments only.
  - No committed credential values, webhook URLs, raw payloads, recording URLs, or presigned URLs were found in imported targets.
- Test gate:
  - `cd apps/data-lake && PYTHONPATH=src python3 -m unittest discover -s tests -v`
  - Result: 13 tests run, 12 passed, 1 skipped because `pyarrow` is not installed locally.
- Syntax gate:
  - `cd apps/data-lake && PYTHONPATH=src python3 -m py_compile scripts/ghl_extract_raw.py scripts/build_curated_tables.py src/gold_coast_data_lake/*.py tests/test_extractor.py tests/test_curated.py`
  - Result: passed.

## Deferred

- Full Parquet runtime verification remains deferred until `pyarrow` is installed in the app environment or the future container environment exists.
- Production refresh runner, AWS locks/status/logging, ECS/Fargate packaging, EventBridge schedule, and alerting belong to later slices.
